@if (_loading == true)
{
    return;
}
@* 
do not show this as isfixed.  because otherwise, when statehaschanged, then does not rerender the child components (wrong)
*@
<CascadingValue Value="this">
    @ChildContent
</CascadingValue>